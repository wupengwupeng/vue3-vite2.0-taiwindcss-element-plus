import { App } from 'vue'
// const components = import.meta.globEager('./*/index.ts')

// export const installAllComponents = (app: App) => {
//   for (const item in components) {
//     for (const key in components[item]) {
//       app.use(components[item][key])
//     }
//   }
// }
