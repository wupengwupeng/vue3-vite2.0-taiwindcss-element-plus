// import VueGL from 'vue-gl';
// import { App } from 'vue'
// export const install = {
//   install(Vue: App, options: any) {
//     Object.entries(VueGL).forEach(([name, component]: any) => Vue.component(name, component));
//   }
// }